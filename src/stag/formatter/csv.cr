class Stag::Formatter::CSV < Stag::Formatter::Base

  def call
    #::CSV.build(@options.csv_separator) do |csv|
      #@data.each do |data_row|
        #csv.row do |csv_row|
          #data_row.each { |item| csv_row << item }
        #end
      #end
    #end

    "TODO" # TODO
  end

end

